export VAGRANT_DATA=/vagrant_data
cd $VAGRANT_DATA
printf "\n\n You are now inside Vagrant Developer Box \n\n"
echo -e " ... on $(pwd) \n"

# this line shouled be last, else above scripts won't work
zsh
. aliases

